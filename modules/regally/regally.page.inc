<?php 

/**
 * File is used for creating the pages
 */

/**
 * Callback function for gallery
 */
function regally_page($tid = 0){
  // Every gallery page consists of two sections:
  // - links to subgalleries
  // - images in current gallery
  // Each of these sections is created using a view, so here we simply get each
  // view (with arguments), and render each, returning the output.
}