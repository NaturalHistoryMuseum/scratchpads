fix forms submissions conflict when multiple nodes are displayed on same page.

add AJAX uploader progress functionality

add file upload restriction control
  * Potential php.ini setting to adjust
  * ini_set('post_max_size', '601M');
  * ini_set('upload_max_filesize', '600M');
  * ini_set('max_input_time', '300');
  * ini_set('max_execution_time', '300'); // 5 minuites

save and display created by file info